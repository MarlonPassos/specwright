## 1. <!-- nome do grupo de tarefas -->

- [ ] 1.1 <!-- descrição da tarefa, incluindo como ela é verificada -->
- [ ] 1.2 <!-- descrição da tarefa, incluindo como ela é verificada -->

## 2. <!-- nome do grupo de tarefas -->

- [ ] 2.1 <!-- descrição da tarefa, incluindo como ela é verificada -->

<!-- Uma tarefa concluída pode registrar, em linhas indentadas logo abaixo do
     próprio box, o que foi de fato executado: `cmd:` com o comando, `resultado:`
     com a saída dele como ela saiu, `em:` com o momento. (O exemplo completo
     está na instrução do artefato; um box de exemplo aqui seria contado como
     tarefa de verdade nesta change.)

     Opcional e retrocompatível: uma tarefa sem o bloco funciona como sempre.
     Não é prova - quem marca o box escreve estas linhas também. É
     reprodutibilidade: o `cmd` pode ser rodado de novo e comparado com o
     `resultado`. -->

<!-- Numa change com parallel: true no .change.yaml, uma tarefa pode terminar
     com tags opcionais entre crases - `files: caminho/a.ts, caminho/b.ts` e
     `depends: 1.1` - para participar do agendamento de dispatch paralelo
     isolado por worktree. Nunca obrigatório: uma tarefa sem tags continua
     funcionando exatamente como sempre funcionou. -->
